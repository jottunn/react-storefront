query getPromotionById ($id: ID!) {
    promotion (id: $id){      
        name
        endDate
        rules {
            cataloguePredicate   
            channels {
                slug
            }           
        }        
    }        
}
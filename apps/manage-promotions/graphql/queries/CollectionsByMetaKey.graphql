query CollectionsByMetaKey($filter: CollectionFilterInput) {
  collections(first:100, filter: $filter) {
    edges {
        node {
            id
            name
            metadata {
              key
              value
            }
        }
    }
  }
}

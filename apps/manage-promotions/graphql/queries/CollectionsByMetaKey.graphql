query CollectionsByMetaKey($filter: CollectionFilterInput, $channel: String!) {
  collections(first:200, filter: $filter, channel: $channel) {
    edges {
        node {
            id
            name
            metadata {
              key
              value
            }
            privateMetadata {
                key
                value
            }
        }
    }
  }
}

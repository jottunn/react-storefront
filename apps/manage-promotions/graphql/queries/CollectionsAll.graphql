query CollectionsAll($channel: String!) {
  collections(first:90, channel: $channel) {
    edges {
        node {
            id
            name
            slug
            metadata {
              key
              value
            }
        }
    }
  }
}

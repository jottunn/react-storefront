query GetProductVariantBySku($sku: String!) {
    productVariant(sku: $sku) {
        id
        sku
        product {
            id
        }
        quantityLimitPerCustomer
        stocks {
            warehouse {
                slug
            }
            quantity
        }
    }
}
fragment CategoryBasicFragment on Category {
  id
  name
  slug
  translation(languageCode: $locale) {
    id
    name
  }
  metadata {
    key
    value
  }
  ancestors(first:5) {
    edges {
      node {
        id
        name
        slug
         translation(languageCode: $locale) {
          id
          name
        }
      }
    }
  }
}

fragment DeliveryMethodFragment on ShippingMethod {
  id
  name
  description
  translation(languageCode: $locale) {
    id
    name
  }
  price {
    ...PriceFragment
  }
  minimumDeliveryDays
  maximumDeliveryDays
  translation(languageCode: $locale) {
    id
    description
  }
}

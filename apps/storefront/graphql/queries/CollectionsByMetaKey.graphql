query CollectionsByMetaKey($filter: CollectionFilterInput, $locale: LanguageCodeEnum!, $channel: String!) {
  collections(first:90, filter: $filter, channel: $channel) {
    edges {
        node {
            ...CollectionDetailsFragment
        }
    }
  }
}
